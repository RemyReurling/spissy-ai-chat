[build]
  publish = "."

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[settings]
  # Enable clean URLs
  clean_urls = true

[dev]
  functions = "netlify/functions"
  publish = "." 